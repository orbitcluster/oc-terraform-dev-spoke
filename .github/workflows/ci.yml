---
name: "hub-dev-spoke"

on:
  workflow_dispatch:

permissions:
  contents: read
  id-token: write
  issues: write

jobs:
  dev-spoke-ci:
    uses: "orbitcluster/oc-terraform-module-eks-setup/.github/workflows/main.yml@v2.14.2"
    with:
      tfvar_file_path: "dev-spoke.tfvars"
      bucket_name: "oc-backend-dev-spoke"
      approvers: "dilipsamanta, mav-sk"
      master_s3_directory: "oc-eks-dev-spoke"
      module_ref: "v2.14.2"
    secrets: inherit
